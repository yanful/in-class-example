{
  "name": "in-class-execise",
  "main": "index.js",
  "scripts": {
    "start": "web-dev-server --node-resolve --open --watch",
    "build": "rollup --config",
    "deploy": "npm run build && gh-pages -d dist"
  },
  "dependencies": {
    "@web/dev-server": "^0.1.35",
    "p5": "^1.5.0",
    "three": "^0.149.0"
  },
  "devDependencies": {
    "@rollup/plugin-node-resolve": "^15.0.1",
    "@web/dev-server": "0.1.35",
    "gh-pages": "^5.0.0",
    "rollup-plugin-copy": "^3.4.0"
  }
}
